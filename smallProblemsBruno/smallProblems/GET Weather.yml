info:
  name: GET Weather
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5256/weatherforecast
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
